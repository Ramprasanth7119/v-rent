"use client";

/**
 * V-RENT Phase 1 component kit.
 *
 * The kit is split by concern under `./ui`; this barrel keeps the historical
 * import path working so screens can import from one place.
 */

export * from './ui';
